node_modules/
dist/
unpackage/
.hbuilderx/
.vscode/
.idea/

# TypeScript
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local
.env
.env.local
*.local

# OS
.DS_Store
Thumbs.db

# Backup files (scripts/ 自动生成的 *.{csv,json,...}.bak)
*.bak

# 本机 vite 临时日志（开发启动时落在仓库根 / realty_app/，git status 会噪音）
.tmp-vite-*.log
realty_app/.tmp-vite-*.log

# coverage 报告（跑 test:coverage 产生的中间产物；CI 单独上传 artifact）
coverage/
realty_app/coverage/

# v1.121.1 路径 C — 本机生成的签名 keystore（不入仓；base64 入 GH Secrets）
realty-release.keystore
*.b64
docs/*.b64.example.txt