# v1.122.44 snapshot 周快照规则单测覆盖（+22 tests）

**日期**: 2026-09-12 凌晨
**head**: 待 commit（已 bump）

## 背景

`src/rules/snapshot.ts`（103 行）是 weekly_snapshot 规则——对应 Python
端 `realty/backend/app/services/snapshot_service.py`。注释明确：
输入"按 crawl_date desc 排序后的 unit_price 列表"，输出 avg / median /
listingCount / coverageScore / dataPolicy。

但 tests/ 目录**没有 `rulesSnapshot.test.ts`**——本会话 v1.122.42 audit
识别 `rules/` 目录 4 文件 0 测试（listingScoring / schoolScoring / scoreUtils / snapshot），
v1.122.43 修了 scoreUtils，本次接着修 snapshot。

## 加的测试

`tests/rulesSnapshot.test.ts`（22 tests，覆盖 4 个函数）：

- **median** (6)：空数组返 0 / 单元素 / 奇数取中 / 偶数取均 / 负数 / 不修改原数组
- **computeWeekWindow** (5)：正常 / 跨月 / 跨年 / 闰年 / 非闰年
- **generateWeeklySnapshot** (11)：
  - 全过滤（< min / > max / 空数组）→ null
  - samples < minSamplesForLatest → median_robust
  - samples 充足 + |latest-median|/median ≤ 0.25 → latest_non_null
  - samples 充足 + deviation > 0.25 → median_robust
  - coverageScore < 1.0（samples < 30）/ = 1.0（samples ≥ 30）
  - weekStartDate 由 computeWeekWindow 派生
  - communityId 透传
  - sourcePriorityUsed 固定为 listing_detail_unit_price

## 验证

- `npm run type-check`：PASS
- `npm test`：**148 文件 / 1258 tests** 全过（vs v1.122.43 147/1236 +1 file +22 tests）

## 关联

`rules/` 目录剩余 2 文件未覆盖：listingScoring.ts (335 行)、
schoolScoring.ts (165 行)。下次按需继续。