# v1.121.117 — 价格 Tab 验收与 App 修复

日期：2026-07-26

## 问题

总览金刚区「价格」点了没反应。根因：Tab 过滤只写 `document.body.setAttribute("data-dash-tab")`，**App 无 document**；且 `dash-tabs` 埋在长页中部，顶部无反馈。

## 变更

1. 页面根 `.page` 绑定 `:data-dash-tab` / `city-scoped`；CSS 以 `.page[data-dash-tab]` 为主（H5 双写 body）。
2. `dash-tabs` 上移到首页入口下方并 sticky；`setDashTab` 统一 toast + 滚到 `#dash-tabs`。
3. 新增验收专册 `docs/DASHBOARD_TABS_ACCEPTANCE.md` + `src/local/dashTabs.ts` + `tests/dashboardTabs.test.ts`（L/U/F 矩阵，失败即未验证成功）。
4. `smoke_dashboard_tabs.mjs` 增加金刚区「价格」路径。

## 验证（对着验收表）

- `npm test -- --run tests/dashboardTabs.test.ts` ✅
- `npm run type-check` ✅
- `npm test` ✅ 1010
