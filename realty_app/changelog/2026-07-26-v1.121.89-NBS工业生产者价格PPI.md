# v1.121.89 — NBS 工业生产者价格（PPI，含建材分项）

**日期**：2026-07-26  
**versionCode**：210

## 变更摘要

1. **新增 NBS PPI**：`scripts/crawl_nbs_ppi.py` + `static/nbs_ppi.csv` + `src/local/nbsPpi.ts`；首页「全国 · 工业生产者价格」卡展示 PPI 同比/环比、购进同比与**非金属矿物制品业**（建材相关）同比；明示 ≠房价。
2. 回填 2026-01…06 共 6 期；兼容「降幅收窄 / 由降转涨」等非常规标题。
3. **月度 CI** 增加 `crawl_nbs_ppi.py` 与 `nbs_ppi.csv`。
4. 测试：`nbsPpi.test.ts`。

## 验证

- `npm run type-check` ✅
- `npm test` ✅（发版前全量）
- E2E smoke：⚠️ 未跑
