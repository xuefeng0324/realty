# v1.121.28 — 标签组合 / 分区地铁 / 7-ELEVEn

## 改动

1. **标签组合**：`getTagCombinationPopularByCity` + `getTagCombinationCrossCityByTag("地铁可达")`
2. **规划地铁**：`getMetroPlanningByDistrict`（优先学区溢价 Top 区）
3. **周边商业**：`getPoiCommercialByPoiTypeLeaderboard("7-ELEVEn")` 本市近距 Top
4. **Bugfix**：`communityIdsInCity` 改回按 `getCommunitiesByCity`（此前误用 poi 列表，导致派生过滤偏窄）

## 数据

- 广州库存 API 直连 403（脚本带 Referer 仍停 07-24）
- 无新公开结构化 diff

## versionCode

`1.121.28` / `149`
