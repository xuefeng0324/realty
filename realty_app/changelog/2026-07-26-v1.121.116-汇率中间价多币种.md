# v1.121.116 — 汇率中间价多币种

日期：2026-07-26

## 变更

1. **RMBQuery 扩列**：`crawl_safe_usd_mid.py` 解析官网表「美元/欧元/日元/港元/英镑」；种子 359 交易日均含欧元与港元。
2. **模块**：`safeUsdMid.ts` 增加 `eurPer100` / `hkdPer100` / `jpyPer100` / `gbpPer100` 与 `getSafeFxMidDelta`。
3. **仪表盘**：原「美元中间价」卡改为「💱 汇率中间价」，展示美元/欧元/港元及日元辅文。
4. **口径**：中间价 ≠ 房价三轴（挂牌/网签/70城）。

## 验证

- `npm run type-check`
- `npm test`（含 `safeUsdMid` 扩样断言）
