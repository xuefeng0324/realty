# v1.121.88 — NBS 居民消费价格（含居住/房租）与收入回填

**日期**：2026-07-26  
**versionCode**：209

## 变更摘要

1. **收入回填**：`crawl_nbs_income.py` 增加 2025 年报/前三季度/上半年/一季度 URL；`nbs_income.csv` 增至 **6 期**，首页趋势可对比跨年。
2. **新增 NBS CPI**：`scripts/crawl_nbs_cpi.py` + `static/nbs_cpi.csv` + `src/local/nbsCpi.ts`；首页「全国 · 居民消费价格」卡展示全国 CPI 同比/环比与**居住 / 租赁房房租**分项（明示房租 ≠ 房价）。
3. **月度 CI** 增加 `crawl_nbs_cpi.py` 与 `nbs_cpi.csv`。
4. 测试：`nbsCpi.test.ts`、收入趋势断言更新。

## 验证

- `npm run type-check` ✅
- `npm test` ✅ 896/896
- E2E smoke：⚠️ 未跑
