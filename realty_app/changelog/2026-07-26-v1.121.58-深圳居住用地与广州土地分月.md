# v1.121.58 — 深圳居住用地接入 + 广州土地分月

## 背景

上轮建议：继续探深圳土地镜像；广州土地卡加分月汇总；城市标签异步回退。

## 深圳居住用地（已成交）

- 打通 `https://szggzy.com/cms/api/v1/trade/content/tk-notice/land-list`
- `landUseLike=居住` + `transactionStatusEnum=YCJ`；**`pageNum` 从 0 起**（从 1 会跳过最新页）
- 字段金额为列表 **起始价**（万元），公开 API 无成交总价；UI 明确标注，≠ 房价均价
- 样本约 **128** 宗；周更 CI 接入 `crawl_sz_land_deals.py`

## 广州土地卡

- `summarizeGzLandDealsByMonth` + 仪表盘分月行（宗数/面积/总价）

## 对抗修复

- dashboard / school / listing-filter 的 `currentCityLabel` 回退 `store.getCityById`（异步 cities 未就绪时不空）

## 仍不可用

- 珠海 PNG / TLS；广州月度批准预售 PNG；深圳成交总价结构化字段仍缺
