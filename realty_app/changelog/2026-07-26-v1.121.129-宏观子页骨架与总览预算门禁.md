# v1.121.129 — 宏观子页骨架与总览预算门禁

> 2026-07-26 · `versionName=1.121.129` · `versionCode=129`

## 背景

`dashboard.vue` 当前 16,746 行 / 14 张 macro-card，远超预算上限（≤ 8 张宏卡）。
直接往总览塞新数据违反「先规划、后实现」原则。

## 本次目标

不再扩张总览 → 把宏观数据按主题拆为 5 个独立子页，先建骨架 + 跑通导航 + 立门禁硬规则。
下轮迭代按子页分批迁 12 张真实数据卡（14 → 8）。

## 改动清单

### 新增页面（5 个骨架）

- `src/pages/macro-rates/macro-rates.vue` — 利率（LPR/MLF/逆回购/Shibor/国债/FR/FDR）
- `src/pages/macro-fx/macro-fx.vue` — 汇市（外储/官方储备/中间价/外汇/结售汇/BOP/IIP）
- `src/pages/macro-industry/macro-industry.vue` — 产业（工业增加值/利润/CPI/PPI/固投/居民收支/PMI）
- `src/pages/macro-region/macro-region.vue` — 区域（广东 房地产简况/经济运行/固投/施工产值）
- `src/pages/macro-trade/macro-trade.vue` — 贸易（海关货物进出口）

### 新增组件

- `src/components/MacroTabNav.vue` — 5 子页共用顶部 tab 切换，emit `change` + `uni.redirectTo`

### 路由

- `src/pages.json` — 注册 5 个宏观子页路由（`navigationBarTitleText` 写中文）

### 入口

- `src/local/homeEntry.ts:55` — 金刚区「宏观」tile 由 `scroll → entry-macro` 改为
  `navigate → /pages/macro-region/macro-region`

### 测试

- `tests/dashboardOverviewBudget.test.ts` — 硬规则更新：
  - 当前 baseline：macro-card ≤ 14（不再涨）+ 行数 ≤ 25 000
  - 远期目标：macro-card ≤ 8 + 行数 ≤ 16 000
  - 文档路径从 `process.cwd()` 改 `__dirname`（修复 vitest 工作目录）
- `tests/macroPages.test.ts`（新建）— 5 子页 smoke 测试：
  - 5 个文件存在 + 含 MacroTabNav + `active="rates/fx/industry/region/trade"`
  - MacroTabNav 组件导出 5 tab key
  - pages.json 注册 5 个路由
  - homeEntry.ts 「宏观」tile 改 navigate

### 文档

- `docs/DASHBOARD_OVERVIEW_BUDGET.md` §2 — 补 `macro-region` 子页（消除文档漏洞：广东 4 张无落位）
- `realty_app/docs/FEATURE_CATALOG.md` §9 — 新增 F-PAGES-MACRO-01..05 五条登记
- `realty_app/DATA_SOURCES.md` — 新增 §7「宏观子页路由」表（含数据源对照）
- `realty_app/README.md` — 版本表新增 v1.121.129 + 更新日志条目

## 验收标准

| 项 | 验收方法 | 当前状态 |
|----|----------|----------|
| 5 子页骨架路由可达 | `pages.json` 注册 + `uni.redirectTo` 跳转 | ✅ |
| MacroTabNav 顶部 5 tab 切换 | 组件 emit change + redirect | ✅ |
| dashboard 长度不再涨 | test: `overviewCards ≤ 14 && lines ≤ 25 000` | ✅（14/16 746） |
| 5 子页 smoke 测试通过 | `npm test -- macroPages` | ✅ |
| 预算文件 + 路由映射文档同步 | `docs/DASHBOARD_OVERVIEW_BUDGET.md` §2 + DATA_SOURCES §7 | ✅ |

## 下轮迭代（todo）

- 迁 4 张广东卡 → `macro-region`（`gdRealEstateBrief` / `gdEconomy` / `gdFaInvestment` / `gdConstruction`）
- 迁 7 张产业卡 → `macro-industry`（`nbsIndustrial` / `nbsIndustrialProfit` / `nbsCpi` / `nbsPpi` / `nbsFaInvestment` / `nbsIncome` / `nbsPmi`）
- 迁 1 张贸易卡 → `macro-trade`（`nbsTrade`）
- 预算测试收紧：macro-card ≤ 8 + 行数 ≤ 16 000（远期目标）