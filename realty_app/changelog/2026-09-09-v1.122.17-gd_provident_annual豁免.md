# v1.122.17 (296) — EXEMPT 白名单加 1 源（gd_provident_annual）

## 背景

v1.122.16 把 9 个年初/季度/月度一次性发布源加入 EXEMPT_FROM_STALE 白名单，
但漏了 `gd_provident_annual.csv`：广东公积金年度报告，每年 5 月初一次
发布（与 gz/sz_provident_annual 同节奏）。

结果：cron 每天 02:30 UTC 仍把 gd_provident_annual (498 天 STALE) 当
真过期推邮件——这是 v1.122.16 我自己明确写在「下一步建议」里要
解决的待办，本轮立即补上。

## 改动

- `.github/workflows/check-csv-freshness.yml`：白名单从 9 个加到 10 个，
  把 `gd_provident_annual.csv` 加到两个 `python scripts/check_csv_freshness.py`
  step 的 `--exempt-from-stale` 参数
- `docs/FRESHNESS_CHECK.md`：白名单表格新增 `gd_provident_annual.csv`
  一行（标记 v1.122.17 加）

## 验证

本地 dry-run：

```
| ⏸ EXEMPT  | `gd_provident_annual.csv` | `publish_date` | 2025-04-29 | 498 |
```

其余真过期源（`gd_construction / gz_housing_plan / sz_land_deals /
sz_planned_supply` 等）继续保留 ❗ STALE 警告。

## 配套

- bump-version.mjs patch: 1.122.16 (295) → 1.122.17 (296)
  同步 manifest.json / config.ts / package.json
- README 版本表新增一行 v1.122.17
- 仅 1 行 workflow yaml + 1 行 docs.md，零代码风险

## 验证状态

- 本地 npm test / type-check: 上一轮 1205/1205 通过，本次未改前端
- 推送后必须等 CI: realty_app tests #288 + build #218 = success
- 9-10 号 02:30 UTC cron 应不再推 gd_provident_annual 的 STALE 邮件
