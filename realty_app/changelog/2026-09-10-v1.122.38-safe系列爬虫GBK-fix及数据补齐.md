# v1.122.38 — safe_* 爬虫 GBK fix + 数据补齐

> 版本号：v1.122.38 (versionCode 317)  
> 日期：2026-09-10  
> 类型：bug 修复 + 数据补齐（patch）  
> 不需要走 FEATURE_QA_PROCESS（无用户可见 UI 改动）

## 背景

v1.122.37 修 shibor/omo_rr/mlf_history 3 个 PBOC 爬虫 GBK bug 后，扫 safe_* 发现：

- **crawl_safe_ora.py**：脚本能跑但 Windows GBK stdout 让 cron job 红
- **crawl_safe_iip / bop / bop_trade / forex / fx_market / settle / usd_mid**：跟 omo_rr/mlf_history 同款 GBK bug，脚本**根本跑不到 print**就 fail

此外 v1.122.37 时我保守回滚了 safe_* 改动——**现在知道 safe_ora 的 source_url 看起来像 2026-02 但其实是 SAFE 2026 年最新数据**（SAFE 站点 URL 反映"页面创建时间"而非"数据时间"——页面 `<meta name="PubDate" content="2026-09-07">`）。

## 改了什么

### 1. 给 8 个 crawl_safe_*.py 批量加 sys.stdout UTF-8 包装

跟 `crawl_omo_rr.py` / `crawl_mlf_history.py` 同款 fix（v1.122.37）：

- `crawl_safe_bop.py` ✅
- `crawl_safe_bop_trade.py` ✅
- `crawl_safe_forex.py` ✅
- `crawl_safe_fx_market.py` ✅
- `crawl_safe_iip.py` ✅
- `crawl_safe_settle.py` ✅
- `crawl_safe_usd_mid.py` ✅（这个脚本用 `import urllib.request` 而非 `from urllib.request import`，特殊处理）
- `crawl_safe_ora.py` ✅（v1.122.37 时已加，本次 commit 合并）

### 2. 重跑补 SAFE 月度数据

| CSV | 改动行数 | 新月份 |
|-----|----------|--------|
| safe_ora.csv | +2 | 2026-08, 2026-07 |
| safe_fx_market.csv | +2 | 2026-07, 2026-06 |
| safe_bop_trade.csv | +2 | 2026-07, 2026-06 |
| safe_bop.csv | +2 | 2026-06 (季度), 2023-06 (回填缺口) |
| safe_iip.csv | +1 | 2025-12 (季度) |
| safe_settle.csv | +1 | 2026-07 |

**所有 source_url 真实指向 SAFE 公告页**，可信。

## 改动文件

```text
modified  realty_app/scripts/crawl_safe_bop.py
modified  realty_app/scripts/crawl_safe_bop_trade.py
modified  realty_app/scripts/crawl_safe_forex.py
modified  realty_app/scripts/crawl_safe_fx_market.py
modified  realty_app/scripts/crawl_safe_iip.py
modified  realty_app/scripts/crawl_safe_ora.py     # (v1.122.37 已修，本次合并)
modified  realty_app/scripts/crawl_safe_settle.py
modified  realty_app/scripts/crawl_safe_usd_mid.py
modified  realty_app/static/seed/safe_bop.csv      # +2
modified  realty_app/static/seed/safe_bop_trade.csv # +2
modified  realty_app/static/seed/safe_fx_market.csv # +2
modified  realty_app/static/seed/safe_iip.csv      # +1
modified  realty_app/static/seed/safe_ora.csv      # +2
modified  realty_app/static/seed/safe_settle.csv   # +1
added     realty_app/changelog/2026-09-10-v1.122.38-safe系列爬虫GBK-fix及数据补齐.md
modified  realty_app/README.md                     # 版本日志 +1 行
modified  realty_app/src/manifest.json             # versionName 1.122.37→1.122.38, versionCode 316→317
modified  realty_app/src/config.ts                 # APP_VERSION 1.122.38
modified  realty_app/package.json                  # version 1.122.38
```

## 没改什么

- ❌ 没改 cron workflow（`crawl-monthly-stats70.yml` 已包含全部 safe_*, 修脚本即可）
- ❌ 没改 frontend / API
- ❌ 没改 frontend

## 一致性

| 检查项 | 结果 |
|--------|------|
| 0 个 frontend 改动 | ✅ 改全是 Python + csv |
| 跑 cron | ✅ 8 个 safe_*.py 现在都能跑（之前6 个直接 print fail） |

## 已知 trade-off

| 维度 | 内容 |
|------|------|
| ✅ 优点 | 8 个 SAFE 爬虫从"Windows 下 cron 必红"变成"可正常运行" |
| ✅ 优点 | 6 个 SAFE CSV 全部补齐最新月度 |
| ✅ 优点 | v1.122.37 误回滚的 safe_* 数据这次正确补上 |
| ⚠️ 缺点 | SAFE 站点慢/waf 时脚本 timeout 45s——cron 应加 timeout 容错 |
| ⚠️ 缺点 | 没有重写爬虫脚本架构，仍是 v0.x 的 urllib.request + re 模式（够用但脆） |
| 🔄 替代方案 A | 重写为 aiohttp + selectolax — 拒绝：scope 巨大 |
| 🔄 替代方案 B | 用 PBOC/SAFE RSS feed — 拒绝：他们不公开 RSS |

## 后续可做

1. 加 PBOC/SAFE 站点健康检查：cron 跑前 GET index page 验证可达 + 模板结构未变
2. 加 retry-with-backoff 模式（已有 `time.sleep` 但无重试）
3. 整理所有 PBOC/SAFE 爬虫的 helper 函数到 `crawl_common.py`（参考 v1.122.27 抽 lianjia helper 模式）