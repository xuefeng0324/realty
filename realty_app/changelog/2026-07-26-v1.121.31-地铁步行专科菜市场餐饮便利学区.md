# v1.121.31 — 地铁本市步行 / 专科医院 / 菜市场派生 / 餐饮·便利·学区溢价

**日期**：2026-07-26  
**versionName**：1.121.31 · **versionCode**：152

## 变更

仪表盘继续接线既有派生 API（不新增爬虫依赖）：

1. **地铁步行可达**：概览卡 + 详情卡增加「本市步行最少 Top」（`getMetroWalkRankingByCityTopN`）
2. **医疗**：专科医院名录（`getHospitalByCityByType(..., "专科医院")`）
3. **菜市场**：派生层最近榜（本市过滤 `getPoiMarketDistanceLeaderboard`）+ 最近小区详情（`getPoiMarketNearestByCommunity`）
4. **周边商业 POI**：最近餐饮 Top（`getPoiCommercialByCityRestaurantNearestByCommunity`）
5. **生活便利度**：分区便利均分 Top（`getLifeConvenienceByCityDistrict`）
6. **商业热度**：最近饭店小区 Top（`getCommunityCommercialByNearest("restaurant")`）
7. **高学区评分房源**：小区挂牌溢价 Top（`getListingSchoolPremiumByCommunityLeaderboard`）

## 验证

- `npm run type-check` 通过
- `npx vitest run` 783/783 通过
- E2E smoke 未跑（本机未起 H5）

## 数据侧说明

公开源刷新仍受阻：`requests` 环境损坏；无 `AMAP_KEY` / `OPENDATA_SZ_TOKEN`。本次仅 UI 接线，未改 seed。
