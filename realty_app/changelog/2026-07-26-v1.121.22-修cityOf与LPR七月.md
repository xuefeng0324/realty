# v1.121.22 — 修 poi cityOf 错分 + 补 2026-07 LPR

## Bug 修复

1. **`poiCommercialRanking.cityOf`**：原先用 `communityId` 区间硬编码推城市，把深圳 1–10、珠海 19–23 错归广州；珠海银行覆盖率恒为 0，广州虚高。改为 `getCommunityById().cityId`。
2. **LPR 新鲜度**：种子停在 `2026-06`，央行已于 2026-07-20 公布（1Y/5Y 仍为 3.0%/3.5%）。补入 `2026-07` 行。

## 测试加强

- 新增 `tests/adversarialRankingProbe.test.ts`：近期仪表盘接线对抗性边界（NaN / 错城 / 覆盖率）
- 新增 `tests/cityOfMismatchHunt.test.ts`：种子快照下 POI 汇总 cityId 必须与 community 一致，且含珠海
- 更新 `poiCommercialRanking` 单测：fixture 显式挂 communities
- 更新 `buildIntegrity` LPR 末行断言 → 2026-07

## versionCode

`1.121.22` / `143`
