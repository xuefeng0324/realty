# v1.121.125 — Shibor 同业拆放利率

日期：2026-07-26

## 变更

1. **爬虫** `crawl_shibor.py`：最新取 `shibor.json`，历史取 `ShiborHis`（按月切片回填）。
2. **字段**：隔夜 / 1W / 2W / 1M / 3M / 6M / 9M / 1Y；来源须为 `chinamoney.com.cn`。
3. **仪表盘**「Shibor」：隔夜 / 1W / 1Y 主 KPI，附 1M/3M/6M；放在国债收益率卡后；脚注明示 ≠ 房价三轴。
4. **CI** 周更 / 月更纳入 `shibor.csv`。

## 口径

Shibor 是银行间同业拆放利率，≠ 挂牌价、≠ 成交价、≠ 网签、≠ 70 城指数；可与 LPR / MLF / 国债收益率对照。

## 验证

- `npm run type-check`
- `npm test`（含 `shibor.test.ts`，2026-07-24 O/N=1.3812 对齐官网 JSON）
