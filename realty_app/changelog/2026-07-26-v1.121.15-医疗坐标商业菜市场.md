# v1.121.15 — 医疗坐标 / 周边商业 / 菜市场可达

## 改动

仪表盘继续消化已有派生、此前未上屏的数据：

1. **🏥 医疗资源** 增补坐标层（`hospitalGeoAnalysis`）：有坐标数、高置信占比、地址分区、市内最近医院对
2. **🏪 周边商业**（`poiCommercialRanking`）：餐饮/银行/便利店 KPI、步行便利 Top、最近银行 Top（按 `community.cityId` 过滤，避开模块内粗略 cityOf）
3. **🥬 菜市场可达**（`poiMarketRanking`）：当前城市最近 / 最远 Top

## versionCode

`1.121.15` / `136`
