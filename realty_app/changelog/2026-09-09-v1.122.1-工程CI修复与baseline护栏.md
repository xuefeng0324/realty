# 2026-09-09 v1.122.1 — 工程 CI 修复与 baseline 同步护栏

修复一批工程 CI 链路上的隐患，**对 App 功能零影响**（versionCode 279 → 280，versionName 1.122.0 → 1.122.1）。建议用户主动点一次「检查更新」以重建 OTA 索引。

## 改动

- **E2E 视觉回归 baseline 过期修复**：补 `realty_app/tests/e2e/artifacts/baseline.png`（从 6409px → 1092px，对齐 e060d99 重构后的精简首页）。原 commit 只刷了参考截图，忘了刷 CI 门禁用的 baseline，导致连续 21 天 cron 失败邮件
- **新增 baseline 同步护栏**（`.github/workflows/realty-app-e2e.yml`）：总是跑，对比 `src/**` 与 `baseline.png` 的最后提交 SHA，stale 时 fail 并打印本地修复步骤——避免再次出现「重构忘刷 baseline」类隐式告警
- **APK 云打包 job 加 `continue-on-error: true`**（`.github/workflows/build-app-wgt.yml`）：可选通道默认允许失败；「检查 DCloud 凭据」step 显式 `exit 0`，避免 ubuntu-latest runner 升级后 `$GITHUB_OUTPUT` 偶发 ENOENT 导致 wgt OTA 发布被阻塞
- **AGENTS.md 强化「提交 / push 即时报告」**：commit 或 push 落地后，agent 必须在下一条对话可见消息里立刻贴 commit 列表 + 远端状态 + 触发的下游 workflow；网络抽风未推也要报，不准让用户翻 GitHub 邮箱/网页

## 验证

- 本地：`build:h5` → `smoke.mjs` PASS → `visual-diff.mjs` byte-match PASS
- CI：等 push 后 `realty_app e2e` 和 `realty_app build (wgt + apk)` 自动跑一轮验证

## 数据源

无数据变更。仅 OTA 平台（jsDelivr CDN）和 CI 工程链路。

## 来源

- git log：faa7bfb / 686bc5b / 8bdaf07 / 8aa3db7