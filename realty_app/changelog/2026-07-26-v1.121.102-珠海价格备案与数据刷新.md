# v1.121.102 — 珠海商品房价格备案 + 公开数据刷新

**日期**：2026-07-26  
**versionCode**：223

## 变更

### 数据接入

- 新增 `scripts/crawl_zh_price_filing.py`：抓取珠海住建局「商品房价格备案公示」列表（`index.html`…`index_N.html`）与详情 HTML 表摘要。
- 种子 `static/zh_price_filing.csv`（本轮 36 条近期公示，约 1971 套）。
- `src/local/zhPriceFiling.ts` + 仪表盘卡「📑 珠海商品房价格备案」（仅珠海）；脚注明示 **备案价 ≠ 挂牌/成交/网签/70城**。
- 周更 CI（`crawl-weekly.yml`）纳入该爬虫（默认 `--max-pages 4`）。

### 数据刷新 / 防护

- 广州新房库存补入 **2026-07-25**。
- 深广网签日更 `--merge` 默认开启（防窗口覆盖历史）；本轮 merge 至 928 行并重建区级周热力。
- LPR 历史触达更新；NBS 居民收入/社消零售脚本再跑确认。

### 文档

- `DATA_SOURCES.md`、`OPTIMIZATION_BACKLOG.md`：`data-zh-price-filing` ✅。

## 口径

备案公示均价为政府销售价格备案，**不是**成交均价，也不得当作城市挂牌均价展示。

## 验证

- `npm run type-check`
- `npm test`（含 `zhPriceFiling.test.ts`）
