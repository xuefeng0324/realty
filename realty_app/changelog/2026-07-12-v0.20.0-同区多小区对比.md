# v0.20.0 — 同区多小区对比 (trend-8)

- 在 dashboard 「区/板块对比」卡上点击任一区，下方展示该区所有 community 的横向对比柱状图
- 数据源：listings.csv 按 community 聚合的均价 + 挂牌数 (复用 `snapshotForCommunityAtWeek`)
- 新 query: `getCommunityCompareByDistrict({ cityId, weekEnd, districtName, metric })`
- 新接口 `DistrictCommunityCompareResponse` (含 districtName, weekEnd, total, items[])
- 新 E2E smoke: `tests/e2e/smoke_district_compare.mjs`
- buildIntegrity 新增 5 条测试
- 单测 246 passed (+5), type-check 干净, 全量 21 个 E2E smoke 通过

UI:
- 卡标题: "📊 {districtName} · {cityName} 小区对比"
- 显示 周快照日期 + total 小区数
- 每行: 小区名 + 横向柱 (bar-fill 长度比例 = 均价/最大值) + 元/㎡
- 点击行 → 小区详情页
- "✕ 关闭" 按钮收起
- 挂牌数 < 3 时显示 ⚠️ 提示