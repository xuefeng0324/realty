# v1.122.31 — 修 2 个 mot / nbsGdp 测试硬断言（防御 cron monthly 自动 commit 后 break）

**日期**：2026-09-10
**类型**：test（防御性维护，v1.122.29 / v1.122.30 续作）
**影响**：2 个 test files / 6 个 tests；下个 cron monthly 自动 commit 后 CI 不会红

## 修了什么（2 files）

| 文件 | 旧断言 | 新断言 |
|------|--------|--------|
| `tests/motTransport.test.ts:15-31` | `period === "2026-H1"` + 10 个数值 toBe + 标签验证 | `period regex /^-H[12]\|-MM\|$/` + 10 个数值范围 + 标签 regex |
| `tests/nbsGdp.test.ts:15-42` | `period === "2026-H1"` + 18 个数值 toBe + 标签验证 | `period regex /^-Q[1-4]\|-H[12]?$/` + 18 个数值范围 + 标签 regex |

## 修法（v1.122.29 / v1.122.30 pattern）

- period 改 regex（`/^20\d{2}(-Q[1-4]|-H[12])?$/` 或 `/^20\d{2}(-(0[1-9]|1[0-2])|(-H[12]))?$/`）
- 数值断言改 `toBeGreaterThan / toBeLessThan` 合理范围
- 季度 GDP 字段 null 用 `if (latest!.quarterGdpYiYuan !== null)` 条件跳过
- 标签输出验证改 regex（不验证具体值）

## 验证

| 验证项 | 结果 |
|---|---|
| 2 个修过的测试 files | ✅ 6/6 全过 |
| `npm run type-check` | ✅ 0 errors |
| `npm test` | ✅ 145/1205 全过 |

## 改动文件

- `modified  realty_app/tests/motTransport.test.ts`
- `modified  realty_app/tests/nbsGdp.test.ts`

合计 2 modified，+56/-26 = +30 net

## bump

`1.122.30 → 1.122.31`（patch：test-only 防御性维护）

## 故意不 commit 的

- 各 `_probe_*.py` 临时探针（仍 untracked）
- v1.122.25 post-fix GBK unlock 报告（untracked）
- PBC 5 个 CSV +10 行数据增量（等用户拍板）
- 其他仍硬编码具体数值的真读 csv 测试（gdRetail / gdServices / gdRealEstateBrief / szAffordableProjects / gzAffordableTargets / nbsAvgWage / chinaBondYield / repoFixing / shibor / omoRrHistory / safeForex / safeUsdMid / nbsCpi 等）——scope 更大，留待后续 batch 处理