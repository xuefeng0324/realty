# v1.122.48 src/local/ 小 helper 单测覆盖（+36 tests）

**日期**: 2026-09-12 凌晨
**head**: 待 commit（已 bump）

## 背景

`src/local/` 目录 13 个文件 0 测试覆盖。本轮挑 4 个**纯函数小 helper**
文件合并到 1 个测试文件（37 行代码 = 36 个用例）：

- `dataMode.ts` (17 行)：`normalizeDataMode` + 常量
- `listingSource.ts` (21 行)：`normalizeListingSourceKind` + `listingSourceKindLabel`
- `listingFilterMatch.ts` (38 行)：`matchesListingTypeFilter` + `matchesDecorateTypeFilter` + 选项
- `dashTabs.ts` (39 行)：`cardVisibleOnDashTab` + `dashTabSwitchFeedback` + 常量

## 加的测试

`tests/localSmallHelpers.test.ts`（36 tests）：

- **dataMode** (4)：'csv-url' / 'seed' / 其它字符串 fallback / null-undefined-number-object fallback
- **listingSource** (9)：REAL/DERIVED/ESTIMATED 直接识别 / 大小写不敏感 / label 含 '派生|样本|住建局公开成交' → DERIVED / label 含 '链家|安居客' → REAL / UNKNOWN / 4 个 label 中文
- **listingFilterMatch** (18)：6 + 4 个选项常量 / matchesListingTypeFilter 8 个边界（含 '在售' / '挂牌在售' 并集逻辑）/ matchesDecorateTypeFilter 4 个边界
- **dashTabs** (5)：DASH_TAB_KEYS 5 项 / cardVisibleOnDashTab 5 个边界（含 'all' / 多值 / 纯空格）/ dashTabSwitchFeedback 5 个 label + 固定 scrollSelector

## 验证

- `npm run type-check`：PASS
- `npm test`：**152 文件 / 1395 tests** 全过（vs v1.122.47 151/1359 +1 file +36 tests）

## 关联

跳过 `src/local/` 大文件（`importer.ts` 1377 行、`queries.ts` 3307 行）——
工作量太大，留给后续。`local/` 还剩 9 文件未覆盖。