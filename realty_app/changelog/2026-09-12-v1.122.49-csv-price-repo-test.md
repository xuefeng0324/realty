# v1.122.49 src/local/ csv / priceSemantics / repoFixing 单测（+37 tests）

**日期**: 2026-09-12 凌晨
**head**: 待 commit（已 bump）

## 背景

`src/local/` 目录剩余 9 文件 0 测试覆盖。本轮挑 3 个**真业务逻辑**文件合并到 1 个测试文件（217 行代码 = 37 个用例）：

- `csv.ts` (76 行)：`parseCSV` (RFC 4180 CSV 解析 + BOM 处理) + `rowsToObjects`
- `priceSemantics.ts` (59 行)：三轴标签 + 禁用词审计
- `repoFixing.ts` (82 行)：`mapRow` 过滤 + 排序 + delta 计算

## 加的测试

`tests/localCsvPriceRepo.test.ts`（37 tests）：

- **csv** (13)：
  - parseCSV (12)：简单 / BOM（**\ufeff 去除—— source 注释里说"不去 BOM 让所有匹配都失败"**）/ CRLF / CR / 引号含逗号 / 引号含换行 / 双引号转义 / 末尾无换行 / 末尾空行不产生空 row / 单行单字段保留 / 空字符串 → 空数组
  - rowsToObjects (3)：rows<2 → [] / header + slice / 行短于 header
- **priceSemantics** (12)：3 个 priceAxisLabel / 4 个短标签 / auditListingPriceCopy 7 个边界（含「成交价」「成交均价」「网签均价」「真实成交价」4 个禁用词 + 干净文案 + 多禁用词）/ priceAxesDisclaimer 含三轴说明
- **repoFixing** (12)：loadRepoFixingFromCSV 排序 / mapRow 过滤 3 个分支（日期格式 / source_url / fr007 或 fdr007 > 0）/ n() 处理千位逗号 / getRepoFixing 返拷贝 / getLatestRepoFixing / getRepoFixingDeltaVsPrev 4 个边界（含 pp 精度 0.0001 / <2 行返 null）

## 验证

- `npm run type-check`：PASS
- `npm test`：**153 文件 / 1432 tests** 全过（vs v1.122.48 152/1395 +1 file +37 tests）

## 关联

`local/` 还剩 6 文件未覆盖（按 ROI 排序）：
- `seedSnapshot.ts` (176 行) / `snapshotLoader.ts` (84 行)
- `metro.ts` (72 行) / `homeEntry.ts` (236 行)
- `importer.ts` (1377 行) / `queries.ts` (3307 行) — 太大

**`csv.ts` BOM 处理有专门的注释警告**：不去 BOM → 整张表所有匹配都失败。本测试覆盖了。