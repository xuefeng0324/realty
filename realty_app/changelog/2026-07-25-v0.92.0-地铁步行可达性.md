# v0.92.0 —— 地铁步行可达性派生 + dashboard mini 卡

> 2026-07-25 · 自检：manifest versionName 0.92.0 / versionCode 93 ·
> config.APP_VERSION 0.92.0 / README 已新增此条。

## 改了什么

1. **派生（`src/local/metro.ts`）**
   - `summarizeMetroWalkAccessibility()`: 按 cityId 分组统计
     * `within5Min` / `within10Min`（步行 ≤5min / ≤10min 的小区数）
     * `pct5Min` / `pct10Min` = 比例
     * 按 `pct5Min` 降序，让"地铁最友好"的城市浮到顶
   - `getMetroWalkRankingTopN(limit)`: 全市场步行最少 Top N
   - `getMetroWalkRankingByCityTopN(cityId, limit)`: 单城市步行最少 Top N
   - 全部派生自已有 `snapshot.metroWalks`（深广 35+ 行）

2. **dashboard 卡（`src/pages/dashboard/dashboard.vue`）**
   - 「🚶 地铁步行可达性」卡：
     * 网格列出每个城市 "≤5min / ≤10min 覆盖比"
     * 全市场步行最少 Top 3（小区 → 站 名 + 分钟数）
     * 当前已记录 35 个小区
   - 给 v0.91.0「70 城近 12 月同比趋势」卡补 `tap-target` + `@click="goStats70"`，
     使点空白处也能跳 stats70 页（之前仅卡标题可点）

3. **`.gitignore`**
   - `.tmp-vite-*.log` / `realty_app/.tmp-vite-*.log` — 本机 dev server 临时 stderr/stdout 日志
   - `coverage/` / `realty_app/coverage/` — `test:coverage` 中间产物（CI 单独 upload artifact）

## 数据真实性

- 派生源：`static/seed/metro_walk.csv`（35+ 行：community_id, city_id, walk_minutes, source）
- 不抓虫；不改 CSV；不动 quota 受限的 AMAP_API
- test:coverage 是覆盖率统计产物，已被 CI 上传 artifact，**不入 git**

## 验证

- `npm run type-check`: ✅ 通过
- `npm test`: ✅ 31 个测试文件 / 539 个用例通过（+4 metro）
- `npm run build:h5`: ✅ DONE Build complete.

## 配套产物

| 文件 | 状态 |
|------|------|
| `src/local/metro.ts` | 新增 |
| `src/pages/dashboard/dashboard.vue` | 加 import / 模板 / computed / cityNameForId helper；v0.91.0 卡补 tap 跳转 |
| `tests/metro.test.ts` | 新增 4 用例 |
| `.gitignore` | 加 .tmp-vite-*.log + coverage/ |
| `src/manifest.json` | 0.91.0/92 → 0.92.0/93 |
| `src/config.ts` | APP_VERSION 同步 |
| `README.md` | 版本表新增此条 |
