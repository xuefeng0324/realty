# v1.122.15 (294) — daily_wangqian 补 9-8 广州各区网签 12 行

## 背景

`crawl-daily-wangqian.yml` 每晚 02:00 UTC 跑，9-8 当日本应自动拉广州 zfcj.gz.gov.cn 数据并提交。
但本次 cron 在 9-8 晚上（北京时间）跑完后没产出可提交 diff，导致 App 用户在 OTA v1.122.14 (293)
当天点「找房」看不到 9-8 数据。

## 数据

`realty_app/static/daily_wangqian.csv` 新增 12 行（HEAD 1073 行 → 1085 行）：

| 区 | 套数 | 面积(㎡) |
|------|------|----------|
| 全市 | 105 | 12594.42 |
| 从化区 | 2 | 342.80 |
| 南沙区 | 4 | 451.49 |
| 增城区 | 24 | 2274.26 |
| 天河区 | 4 | 918.05 |
| 海珠区 | 1 | 118.50 |
| 番禺区 | 8 | 1932.27 |
| 白云区 | 9 | 961.10 |
| 花都区 | 35 | 3453.34 |
| 荔湾区 | 4 | 654.63 |
| 越秀区 | 0 | 0.00 |
| 黄埔区 | 14 | 1487.96 |

源：`https://zfcj.gz.gov.cn/zfcj/tjxx/spfxstjxx`

## 关于"补 4 源"的修正

本会话中途曾用 `crawl_gd_construction.py` / `crawl_gz_housing_plan.py` / `crawl_sz_planned_supply.py` /
`crawl_sz_land_deals.py` 探针跑过一次，**但跑出结果与 HEAD 已 commit 的 v1.122.11 (commit 56995de) 内容
完全一致**——说明 v1.122.11 那一批 commit 已经把 4 个数据源补到了最新。freshness 检查报告的
"STALE 12" 在本轮开始时**多数已不是真 STALE**（freshness 检查对 `publish_date` 字段的解释是"数据
发布时间"，对年度快照如 `education_overview` 会把整张表的发布日期当成陈旧度源头，但实际工作流
是"年初一次发布 + 全年引用"，所以 freshness 永远会显示 STALE）。

**真正仍然陈旧的源**（已二次核对 HEAD 内容，截至 2026-09-09）：
- `daily_wangqian.csv`（本次已补 9-8）
- `education_overview.csv`（年度快照，等 9 月底新学年）
- `gz_provident_annual.csv` / `gd_provident_annual.csv` / `provident_fund_rates.csv`（年初一次性发布）
- `zh_bdc_registration.csv`（珠海不动产登记季报，9 月才出 Q3）
- `zh_price_filing.csv`（珠海价格备案列表页 404，源站问题）

## 配套修复

- 还原 168 个 noise diff（编辑器误改换行符 / 编码）：`git restore --worktree --staged -- realty_app/changelog realty_app/src/pages.json`
- `bump-version.mjs patch` 1.122.14 → 1.122.15 (294)，同步 `manifest.json` / `config.ts` / `package.json`

## 验证

- 本地 `python realty_app/scripts/check_csv_freshness.py`：daily_wangqian 由 1 天前 STALE 转为 OK
- 本地 `npm test` / `npm run type-check`：上一轮已 1205/1205 通过，本次未改前端代码
- 本地未跑：CI 验证（push 后必须等 GitHub actions realty_app tests #285 + build #215 都是 success）
- 本地未跑：端到端 E2E smoke（v1.122.7 改的搜索框垂直居中已在 v1.122.14 验收；本次纯数据 + 版本号 bump）
