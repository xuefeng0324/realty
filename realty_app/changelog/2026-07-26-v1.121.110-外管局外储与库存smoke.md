# v1.121.110 — 外管局外储与库存 smoke

日期：2026-07-26

## 变更

1. **外汇储备**
   - 新增 `crawl_safe_forex.py`：抓取外管局新闻分页「外汇储备规模」通稿 + 官方储备资产 XLSX 补绝对值（公告优先）。
   - 种子 `static/seed/safe_forex.csv`（约 13 期，2025-06～2026-06）。
   - `safeForex.ts` + 仪表盘「💱 外汇储备」卡；脚注明示 ≠ 挂牌/成交/网签/70城。
   - 周更 / 月更 CI 纳入爬虫与 CSV。

2. **库存入口 smoke（test-entry-1）**
   - 选择器改为 `.home-king-tile` / `[data-home-king]`。
   - 断言改为视口相交，避免 sticky 头导致 `boundingBox.y` 误判。

## 口径

外储规模为宏观流动性背景，**不是**房价三轴任一轴。

## 验证

- `npm run type-check`
- `npm test`（含 `safeForex.test.ts`）
- 本机 `smoke_entry_supply.mjs`（需 H5）
