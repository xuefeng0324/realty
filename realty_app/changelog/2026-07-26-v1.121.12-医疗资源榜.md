# v1.121.12 — 仪表盘医疗资源榜 + 深色漏网浅底

## 改动

1. **医疗资源卡**：接入已有 `hospitalRanking.ts`（此前 changelog 写明暂不接 UI）
   - 当前城市：医院数 / 三甲数 / 三甲占比 / 重点医院数
   - 分区医院数 Top 6
   - 等级优先 Top 5（`displayName` / `officialName`）
2. 生活便利度卡行内 chip 改主题 token（深色下不再写死 `#1e293b`）
3. 顺带发布含 v1.121.11 深色统一的 OTA（CI 此前未能及时出 132）

## 数据

`static/seed/hospitals.csv` → store → `summarizeHospitalByCity` / `summarizeHospitalByCityDistrict` / `getHospitalTopByLevelByCity`

## versionCode

`1.121.12` / `133`
