# v1.121.56 — 广州居住用地成交 + 城市名同步修复

## 背景

上轮建议：对抗狩猎 `cityNameForId` 竞态；继续找可结构化数据。

## 确认缺陷（Hunter→Skeptic）

| 级别 | 问题 | 修复 |
|------|------|------|
| P1 | 网签卡 `currentWangqianCityName` 依赖异步 `cities` → 首屏可能空 | 改 `cityNameForId`（含 store 回退） |
| P1 | 70 城当前城排名/趋势/12 月序列同上 | 同上 |
| P1 | 房源页性价比 Pareto 用异步 city_name | `syncCityName` ← store |

## 新数据

- `static/gz_land_deals.csv`：规自局成交公示中的居住/R2 地块（当前样本 5 宗）
- `scripts/crawl_gz_land_deals.py`：列表+详情 HTML 解析（工业地自动跳过）
- 仪表盘广州城：「🗺️ 广州居住用地成交」

**口径**：土地出让成交价 ≠ 房价均价；地表单价未除容积率。

## 测试

- `tests/gzLandDeals.test.ts`（含 city-sync 对抗断言）
