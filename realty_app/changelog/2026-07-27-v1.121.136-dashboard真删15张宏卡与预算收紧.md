# v1.121.136 — dashboard.vue 真删 15 张宏卡 + 预算收紧

**日期**：2026-07-27
**versionCode**：257

## 一句话总结

**首页真瘦身**：dashboard.vue 从 15,392 行 → **14,186 行**（-1,206 行 / -7.8%）。
真删 15 张宏卡的 HTML + script refs/computed + imports，全部进 macro-rates / macro-fx 子页。
预算硬规则收紧到 **≤ 4 宏卡 / ≤ 14,500 行**。

## 背景

前几轮（v1.121.130 → v1.121.135）的「瘦身」实际只是**迁出**宏卡 HTML 到子页，
但 dashboard.vue 内仍保留这些宏卡的完整模板 + v-if + 对应 refs/imports，
导致 dashboard.vue 实质**没变短**。用户反复反馈「第一页还是老长」。

本轮按用户指令「**通过设置入口，不要老是放在同一页**」精神，**真删**。

## 本次实现

### 1. 真删 15 张宏卡（dashboard.vue → 子页）

| 类别 | 删除的卡 | 迁入子页 |
|------|---------|----------|
| 利率 (5) | MLF / OMO / 中债国债 / Shibor / 回购 FR-FDR | `macro-rates.vue` |
| 央行 (2) | 央行金融统计 / 广东地区社融 | `macro-fx.vue`（央行金融统计） |
| 外汇 (8) | 外储 / 官方储备分项 / USD-CNY 中间价 / 外汇市场成交 / 银行结售汇 / 国际收支 / 国际投资头寸 / 货服贸易 | `macro-fx.vue` |

**删除方式**：
- 模板：脚本 `remove_dashboard_macro_cards.js` 定位 562-1628 区间，批量 splice 删除 15 段（-1,053 行）
- script computed/refs：删除 10051-10105 区间的 mlfLatest..safeUsdMidMonthAvg（-55 行）
- script imports：删除 6404-6501 区间的 15 个 `import { ... } from "../../local/xxx"`（-98 行）

### 2. 子页补 4 张新宏卡（用户原有 dashboard 卡的补充）

`macro-fx.vue` 本轮新增 2 张卡：
- **央行金融统计（pbcFinStats）**：8 KPI（社融存量/增量/M2/M1/本外币贷款/住户贷款/汇率/数据源）
- **地区社融 · 广东（pbcRegionSf）**：6 KPI（社融增量/人民币贷款/企业债/政府债/股权融资/排名）
- 补 **USD/CNY 中间价卡**：HKD 100 KPI
- 补 **银行结售汇卡**：涉外收入 KPI

新增 42 KPI 全部用 `MacroKpiCell` 组件渲染。

### 3. 测试同步

**15 个 macro 测试**（mlf/omo/chinabond/shibor/repo/safeForex/safeOra/safeBopTrade/safeIip/safeBop/safeSettle/safeFxMarket/safeUsdMid/pbcFinStats/pbcRegionSf）的「爬虫与仪表盘接线」用例：

- 原断言：`dashboard.vue` 含 `data-xxx` / `getLatestXxx`
- 改为断言：`macro-rates.vue` 或 `macro-fx.vue` 含对应内容

### 4. 预算硬规则收紧

`tests/dashboardOverviewBudget.test.ts`：

| 指标 | 旧硬规则 | 新硬规则 |
|------|---------|----------|
| overview tab 宏卡数 | ≤ 8 | **≤ 4** |
| dashboard.vue 总行数 | ≤ 16,000 | **≤ 14,500** |
| 远期宏卡数 | ≤ 4 | **≤ 2** |
| 远期行数 | ≤ 12,000 | **≤ 10,000** |

### 5. 文档同步

- `docs/DASHBOARD_OVERVIEW_BUDGET.md` §1 量化目标更新；§8 todo 加 T-012（完成）+ T-013/014（待办）
- `docs/FEATURE_CATALOG.md` §9 新增 F-PAGES-DELETE-01（v1.121.136 真删宏卡）
- `realty_app/README.md` 版本表新增 v1.121.136
- `tests/dashboardOverviewBudget.test.ts` 硬规则更新
- 5 个子页 + dashboard 测试同步断言位置

## 验证

- type-check ✅
- 124 文件 / 1082 用例 ✅
- dashboardOverviewBudget.test.ts：4 用例全过（含新收紧的硬规则）

## 后续可做

- **T-013 新建 `pages/data-tools/data-tools.vue`**：把派生卡（学校指标/重点学校/教育事业/行政区划/通勤/户型分布/挂牌结构/区情画像/特征溢价/挂牌标签/地铁规划）分组迁出。理论 -2,000 行。
- **T-014 价格 tab 折叠**：用 ref 控制，把派生卡折叠显示。需配合 T-013 完成。
- **T-015 nbsMacro 派生卡进一步瘦身**：当前 100+ KPI → 拆成「核心 4 + 查看完整 → 子页」
- **T-016 dashboard 剩余非宏卡瘦身**：区/板块对比、区级近 8 周价格趋势、网签周环比等约 20+ 张派生卡

## 备注

- 本轮是**首次真删**而非迁出：dashboard.vue HTML 实际减少 1,053 行
- 子页 KPI 数从 ~141 → **183**（+42）
- 用户原指令「**通过设置入口，不要老是放在同一页**」 → T-013（数据工具页）+ T-014（价格 tab 折叠）是后续主线