# v1.121.115 — 单行控件垂直居中

日期：2026-07-26

## 变更

1. **根因**：`input` 仅有 `min-height`/`height` 无匹配 `line-height`；自定义 `button`（详情底栏、地图筛芯片等）无 `display:flex` + `align-items:center`，在 uni-app 默认 `line-height: inherit` 下文字顶偏。
2. **修复**
   - 房源筛选 / 首页搜索：`height` = `line-height` = 64rpx + `box-sizing: border-box`
   - 详情底栏主/辅 CTA：`inline-flex` 居中
   - 全局 `App.vue` `button` 默认 flex 居中
   - 地图筛/图层芯片、学校/设置输入框一并对齐
3. **测试**：`tests/uiVerticalAlign.test.ts` 源码门禁；验收文档补 U6 / 关键字居中期望。

## 验证

- `npm run type-check`
- `npm test`（含 uiVerticalAlign 7 条）
