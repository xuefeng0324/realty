# v1.121.35 — 派生二轮接线

**日期**：2026-07-26  
**versionName**：1.121.35 · **versionCode**：156

## 变更

上轮建议「继续接线 getLprRange / 散点象限跨城 / summarizeFeaturePremiumByCity」落地：

| API | UI |
|-----|-----|
| `getLprRange` | LPR 卡近 12 月序列 |
| `getCommunityScatterCrossCityByQuadrant("价值洼地")` | 散点卡跨城代表盘 |
| `summarizeFeaturePremiumByCity` | 特征溢价卡本市极值摘要 |
| `getFeaturePremiumByCityDimension(..., "decorate")` | 装修维全部桶 |
| `summarizeMetroPlanningByPhase` | 规划地铁全国分期 |
| `summarizeListingFreshnessByCity` | 新鲜度本市聚合 KPI |
| `getOrientationFloorByCityFloorBucket(..., "高楼层")` | 高楼层各朝向溢价 |
| `getDistributionByCityDimension(..., "精装")` | 精装×楼龄分布桶 |

## 数据刷新尝试

- 已删除损坏的 `distutils-precedence.pth`（含空字节），`python`/`urllib` 可用
- `pip` 本体仍因 site 包空字节无法运行，`requests` 未装上
- 深圳网签 urllib 探测返回 404（接口路径可能变更），本版未改 seed

## 验证

- type-check ✅
- vitest 786/786 ✅
