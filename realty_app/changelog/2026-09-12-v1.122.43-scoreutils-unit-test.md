# v1.122.43 scoreUtils 单测覆盖（+22 tests）

**日期**: 2026-09-12 凌晨
**head**: 待 commit（已 bump）

## 背景

`src/rules/scoreUtils.ts`（47 行）是 listingScoring / schoolScoring 等
核心评分模块的共用工具层。注释明确："对应 Python 端
realty/backend/app/services/score_utils.py，保持字段名完全一致"——
**前后端共用一套逻辑**，测试价值高。

但 `tests/` 目录**没有 `rulesScoreUtils.test.ts`**——规则模块完全无门禁。
本会话发现的 src/ 测试覆盖薄弱的 `rules/` 目录有4 个文件全 0 测试
（listingScoring.ts 335 行最大，scoreUtils.ts 47 行最小先做）。

## 加的测试

`tests/rulesScoreUtils.test.ts`（22 tests，覆盖 5 个函数）：

- **clamp** (7)：正常值 / 小于lo钳 / 大于hi钳 / null / undefined / NaN / lo==hi
- **parseFloorNumber** (3)：正常楼层 / null-undefined-空 / 无数字字符
- **maybeScaleTrendDelta** (2)：abs<=1 乘 100 / abs>1 原样
- **currentYearFloor** (4)：不传参 / null fallback / 指定日期 / vi.useFakeTimers
- **jsonListMax** (6)：正常 / 单元素 / 空数组返 null / null / undefined / 负数

## 验证

- `npm run type-check`：PASS
- `npm test`：**147 文件 / 1236 tests** 全过（vs v1.122.42 146/1214 +1 file +22 tests）

## 关联

本会话连续 2 次给"无测试覆盖的 utils/rules 模块"加单测：
- v1.122.42：dashboardGuide.test.ts 9 tests
- v1.122.43：rulesScoreUtils.test.ts 22 tests

`rules/` 目录剩余 3 个文件未覆盖：listingScoring.ts (335 行)、schoolScoring.ts (165 行)、
snapshot.ts (103 行)。下次按需继续。