# v1.122.45 schoolScoring 学校未来评分 v1 单测覆盖（+17 tests）

**日期**: 2026-09-12 凌晨
**head**: 待 commit（已 bump）

## 背景

`src/rules/schoolScoring.ts`（165 行）实现 `computeSchoolFutureScoreV1`
——学校未来评分 v1 规则，4 因子加权（latest 0.55 / group 0.20 / district 0.15
/ trend 0.10）+ 每个因子有 NULL fallback + previousIndicator 反推 trend delta。

本会话 v1.122.42 audit 识别 rules/ 4 文件 0 测试。v1.122.43 / v1.122.44
修了 scoreUtils / snapshot；本轮接着修 schoolScoring。

## 加的测试

`tests/rulesSchoolScoring.test.ts`（17 tests）：

- **happy path** (4)：所有指标完整时加权聚合 + confidence 1.0 + 贡献展开 + ruleVersion 透传 + explainText 包含 4 因子展开
- **NULL fallback** (6)：latest / group flag / district / trend 各自 null 时的 fallback 值 + 置信度扣分；
  group=false 走"非集团"分支（bonus 40，不扣 confidence）；
  group=true 但 strength null → bonus 80；
  previousIndicator 自动反推 trend delta
- **trend delta 分支** (5)：delta>0 / delta<0 / 已 scale 视作原值 / 极端正（200）clamp / 极端负（-200）clamp
- **全 NULL 边界** (1)：confidence 钳到 [0, 1]、trendScore 仍是有限数

## 验证

- `npm run type-check`：PASS
- `npm test`：**149 文件 / 1275 tests** 全过（vs v1.122.44 148/1258 +1 file +17 tests）

## 关联

rules/ 目录剩 **listingScoring.ts (335 行)** 1 个文件未覆盖。