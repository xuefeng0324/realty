# v1.122.50 snapshotLoader 单测覆盖（+15 tests）

**日期**: 2026-09-12 凌晨
**head**: 待 commit（已 bump）

## 背景

`src/local/snapshotLoader.ts`（84 行）提供 `loadSnapshotFromBase(base, source)`——
用户切换"自定义 CSV 模式"时下载 5 必需 + 36 可选文件，验证数据完整性（cities/communities/listings 非空 + orphan listings 检测），调 `importSnapshot` 合成 snapshot。

本会话 v1.122.42 audit 识别 local/ 13 文件 0 测试，本轮 v1.122.48 / v1.122.49 已修 4 + 3 = 7 文件；本轮接着修 snapshotLoader。

**注**：`seedSnapshot.ts`（176 行）v0.4.0-v1.115.0 累计 37 个 `?raw` import——vitest 不能直接 mock ?raw，本测试只覆盖 snapshotLoader 的纯逻辑分支（4 错误 + 1 happy）。

## 加的测试

`tests/localSeedSnapshotLoader.test.ts`（15 tests）：

- **URL 协议验证** (6)：ftp:// → throw / http:// ok / https:// ok / 尾随斜杠 normalized / 多个尾随斜杠 normalized / 前后空格 trim
- **必需文件 validation** (1)：必需文件 fetch 失败 → throw「完整快照缺少必需文件」
- **snapshot 非空 validation** (3)：cities 空 / communities 空 / listings 空 → throw「缺少城市、小区或房源数据」
- **orphan listing validation** (2)：listing 的 communityId 不在 communities → throw / 全部关联 ok
- **happy path** (3)：**41 次 downloadText**（5 必需 + 36 可选）/ 必需 timeout=20000、 可选 timeout=12000 / source 参数透传给 importSnapshot

## 验证

- `npm run type-check`：PASS
- `npm test`：**154 文件 / 1447 tests** 全过（vs v1.122.49 153/1432 +1 file +15 tests）

## 关联

`local/` 目录剩余 4 文件未覆盖：`metro.ts` (72) / `homeEntry.ts` (236) / `importer.ts` (1377) / `queries.ts` (3307)。

`seedSnapshot.ts` 暂跳过：37 个 `?raw` import vitest 难以 mock。