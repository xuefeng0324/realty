# v1.121.84 · 房源关键字筛选 + 首页搜索贯通

**日期**：2026-07-26  
**versionCode**：205

## 变更

- `ListingFilterFilters.keyword`：匹配标题 / 小区名 / 行政区 / `tagsJson`（子串、不区分大小写）
- 房源 Tab 顶部搜索框；重置清空关键字
- 首页搜索模式「房源」写入 `pending listing query`，`switchTab` 后自动应用（与学校 pending 同模式）
- 学校页改为 `onShow` 消费 pending，避免 Tab 已挂载时丢查询
- 验收：`DASHBOARD_ENTRY_IA` / `FEATURE_CATALOG` F-LIST-01；单测 `listingFilterKeyword` + `homeEntry`

## 验证

- `npm run type-check` ✅
- `npm test` ✅ 886/886
- E2E smoke ⚠️ 未跑（本机未起 H5）
