# v1.122.39 — 修 pbc_fin_stats / pbc_region_sf GBK + 补深圳土地成交

> 版本号：v1.122.39 (versionCode 318)  
> 日期：2026-09-10  
> 类型：bug 修复 + 数据补齐（patch）  
> 不需要走 FEATURE_QA_PROCESS（无用户可见 UI 改动）

## 改了什么

### 1. 修 `scripts/crawl_pbc_fin_stats.py` + `scripts/crawl_pbc_region_sf.py` GBK bug

跟 v1.122.37 (omo_rr / mlf_history) 和 v1.122.38 (8 个 safe_*) 同款 fix：

- `crawl_pbc_fin_stats.py`：sys.stdout UTF-8 包装
- `crawl_pbc_region_sf.py`：sys.stdout UTF-8 包装

**修后验证**：本地跑均跑通，无 GBK 报错。

**为什么漏修了**：
- v1.122.36 / v1.122.37 修 LPR + omo_rr / mlf_history 时，我手动跑了 PBOC 爬虫发现 4 个 fail（pbc_fin_stats / pbc_region_sf / safe_* 6 个）
- v1.122.37 commit时只看了 "哪些 csv 被改了" → 只 commit 了 pbc_fin_stats / pbc_region_sf 的**数据改动**，**没修脚本**（当时数据已经写入就以为 OK）
- v1.122.38 修了8 个 safe_* 但**漏掉了2 个 pbc 脚本**

**这次补完**：nbs + pbc + safe 三类爬虫 GBK 包装全齐。**只剩 amap 系列需要审计**（按需触发，平时 cron不调）。

### 2. 补 `static/seed/sz_land_deals.csv` +1 行

- 新增 2026-08-11 福田安托山四路与侨香三道交汇处成交记录（深土[2026]-[053]，113100万 起拍价，5844.41㎡ 二类居住用地）
- 来自深圳公共资源交易中心 `szggzy.com`

## 改动文件

```text
modified  realty_app/scripts/crawl_pbc_fin_stats.py
modified  realty_app/scripts/crawl_pbc_region_sf.py
modified  realty_app/static/seed/sz_land_deals.csv      # +1 行 (福田安托山 2026-08-11 成交)
added     realty_app/changelog/2026-09-10-v1.122.39-pbc-szland-data-bump.md
modified  realty_app/README.md                          # 版本日志 +1 行
modified  realty_app/src/manifest.json                  # versionName 1.122.38→1.122.39, versionCode 317→318
modified  realty_app/src/config.ts                      # APP_VERSION 1.122.39
modified  realty_app/package.json                       # version 1.122.39
```

## 没改什么

- ❌ 没动 nbs_* / safe_* 脚本（已全 OK）
- ❌ 没动 frontend

## 一致性

| 检查项 | 结果 |
|--------|------|
| npm run type-check | ✅ 0 errors（0 frontend 改动） |
| vitest | ✅ 145 files / 1205 tests 全过 |
| 跑 pbc_fin_stats / pbc_region_sf | ✅ 两个都跑通（之前 GBK fail） |
| NBS 系列 smoke test | ✅ 15 个全 OK（无需 GBK fix） |

## 已知 trade-off

| 维度 | 内容 |
|------|------|
| ✅ 优点 | PBOC 系列爬虫全部 GBK 修复完毕 |
| ✅ 优点 | 福田安托山土地成交数据立刻可用 |
| ⚠️ 缺点 | amap 系列（15 个脚本）未审计 GBK——但 amap 仅按需触发，平时 cron 不跑 |
| ⚠️ 缺点 | 同样 GBK 模式可能在未来新增爬虫中复发——**建议** 给 `crawl_common.py` 加 default 包装让所有用 `from crawl_common import ...` 的爬虫自动获得 UTF-8 stdout |
| 🔄 替代方案 | aiohttp+selectolax 重写 — scope 过大 |

## 后续可做

1. 把 UTF-8 stdout 包装集成到 `crawl_common.py` 默认 import 副作用，避免每个爬虫单独 patch
2. 审计 amap 系列爬虫 GBK 状态（按需触发，但用户本地 Windows 跑会 fail）
3. **整体重构**：把 60+ 个 crawl_*.py 用 `crawl_common.py` 集中 helper（参考 v1.122.27 抽 lianjia helper）