# v1.121.54 — 涨跌色统一 + cityName 回退 + 深圳计划入市

## 背景

延续 Hunter→Skeptic：修上轮遗留色义/城市名；并接入可结构化抓取的深圳住建局「计划入市」季度公示。

## 确认缺陷与修复

| 级别 | 问题 | 修复 |
|------|------|------|
| P1 | `.stats70-up` / `.wangqian-up` / `.wq-trend-up` 涨绿，与 `.trend-up` 涨红冲突 | 统一涨红跌绿 |
| P1 | 网签页 `wangqian.vue` trend-up 为绿 | 对齐仪表盘 |
| P1 | LPR 利率升显示 ↓+绿（箭头与色反向） | `rateDeltaClass` / `rateDeltaArrow` |
| P1 | `cityNameForId` 异步 cities 空时显示 `city#id` | 回退 `store.getCityById` |

## 新数据

- `static/sz_planned_supply.csv`：深圳住建局季度计划入市（2024Q4–2026Q3）
- `scripts/crawl_sz_planned_supply.py`：解析公示**正文**（不 OCR PDF）
- 仪表盘深圳城：「🏗️ 深圳计划入市」卡（供应套数/面积/住宅 + 环比）
- 周更 CI 同步刷新

## 口径声明

计划入市 = 官方公示的**预期供应**，不是成交量、不是可售库存、不是成交均价。

## 测试

- `tests/adversarialColorCitySupply.test.ts`
- type-check + 全量 unit
