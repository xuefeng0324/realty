# v1.121.30 — 散点面积段 / 分区医院 / 区划名匹配

## 改动

1. **散点卡**：`getCommunityScatterByAreaCohort("改善")` + `getCommunityScatterByQuadrant("价值洼地")`
2. **医疗**：`getHospitalByCityByDistrict` 展示分区 Top1 医院名录
3. **行政区划**：`getAdminDistrictByNameLike("新区")` + `getAdminDistrictCrossCityByNameLike("海")`

## 数据

- 广州库存重爬：仍停 **2026-07-24**
- 无 `AMAP_KEY`，天气未刷新

## versionCode

`1.121.30` / `151`
