# v1.122.47 useTrendVisualization composable 单测覆盖（+19 tests）

**日期**: 2026-09-12 凌晨
**head**: 待 commit（已 bump）

## 背景

`src/composables/useTrendVisualization.ts`（147 行）是 dashboard.vue 与
trend-analysis.vue 共用的 4 张可视化卡的 composable。本会话 v1.122.42 audit
识别 `composables/` 1 文件 0 测试（useTrendVisualization），本轮补上。

## 加的测试

`tests/composablesUseTrendVisualization.test.ts`（19 tests，覆盖 5 个 helper
+ reloadAll 并发）：

- **baMaxCount** (4)：null / 空 grid / 二维 grid / 含负数（异常数据）
- **ofCellLabel** (4)：ratio null / ratio=0 / ratio>0 加 '+' / ratio<0
- **daCellClass** (4)：null / > 0.01 / < -0.01 / 边界 ±0.01（含 0）
- **scatterImproveCohort** (3)：null / 3 cohort 切（value/mid/premium 0.9x/1.1x）/ x 直接是 medianUnitPrice
- **scatterValueDip** (3)：null / 全高于 0.9x / 3/5 低于 0.9x
- **reloadAll** (1)：并发 4 个 reload + trendVizLoading/error 切换正确（mock queries 模块）

## 验证

- `npm run type-check`：PASS
- `npm test`：**151 文件 / 1359 tests** 全过（vs v1.122.46 150/1340 +1 file +19 tests）

## 关联

`composables/` 目录已扫清（1 文件 0 测试 → 1 文件全部覆盖）。

跳过 `src/api/` 5+ 文件（communities/contracts/listings/meta/schools/stats）——
本质是 RPC stub wrapper（只有少量 `?? default` 默认值逻辑），单测 ROI 低
（需要 mock fetch + 断言 endpoint URL/params）。如要测，单独做一个
vi.mock("./http") 的 task 即可，模板见本次 reloadAll 测试。