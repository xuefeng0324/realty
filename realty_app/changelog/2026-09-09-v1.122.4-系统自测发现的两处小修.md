# 2026-09-09 v1.122.4 — 系统自测发现的两处小修（versionCode 283）

用 Playwright 遍历 17 个页面截图自测，找到 2 处视觉/布局小 bug：

## 改动

- **设置页"数据源"卡片末段截断**：`<view class="muted">REAL 为公开页解析的真实挂牌；DERIVED 为分析样本，不代表逐套成交。见 HOUSING_PRICE Phase C/D。</view>` 在 390dp viewport 下"逐套成交"被挤压；改成 `<text>` + `word-break: break-all` + `white-space: normal` + `display: block`，让长 CJK 文本正常 wrap
- **地图 tab 城市切换按钮溢出**：原 `.city-option { min-width: 96rpx }` 让"深圳 / 广州 / 珠海"在窄屏第三个被裁；改为 `min-width: 0; flex: 1 1 auto`，三个按钮等宽分摊

## 不修（已知非 bug 或设计意图）

- 网签数据"21 天前"：fetch 已 graceful degrade，等明早 cron 自然更新
- 首页 dashboard "值得看看"卡片标题 vs 楼盘名：v1.122.0 设计
- 学校查询结果空：未选学校时正确状态
- 地图 marker 稀疏：真实数据点少

## 来源

- 自测脚本：realty_app/system_test.cjs（17 个页面零 console error）
- 截图：C:/Users/Admin/AppData/Local/Temp/realty_sys/*.png