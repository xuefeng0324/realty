# v1.121.29 — 地铁开通 / 结构中位价 / 医院同名区

## 改动

1. **规划地铁**：`getMetroPlanningByOpenYear(2028)` 全国按里程 + `getMetroPlanningByStatus("在建")` 本市
2. **挂牌结构**：`getDistributionTopByMedianPrice` 本市结构桶中位单价 Top
3. **医疗**：`getHospitalCrossCityByDistrict`（取分区医院数 Top1 区名对照；三城鲜有同名）

## 数据

- 教育概览重爬：内容无变（仅 CRLF），未入库
- 无新公开结构化 diff

## versionCode

`1.121.29` / `150`
