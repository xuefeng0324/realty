# 2026-09-09 v1.122.2 — UI 小修与 wangqian 抓取降级（versionCode 281）

4 处细节修复，**对 App 体验可验证**：

## 改动

- **搜索框垂直居中**：`dashboard.vue` 的 `.home-v122-search-input` height 72 → 64，加 line-height 64；`.home-v122-search-btn` min-height 64 → height 64，加 line-height 64。搜索按钮现在与输入框同一基线
- **行情页 eyebrow 在 iOS safe area 内**：`market.vue` 的 `PageShell` 加 `safe-top`，避免 AppHeader 的 eyebrow 被 iOS status bar 区域遮挡
- **wangqian 抓取 graceful degrade**：`crawl_daily_wangqian.py` 新增 `_post_with_retry`（3 次 + 指数退避 2s/4s/8s），4 个 fetch 函数加 try/except；单城/单接口失败时 warn + 返回空，保留 CSV 既有数据，不再让整 cron 红。**根因：深圳 fdc 接口被阿里云 WAF 持续 412**——脚本层面已尽力；广州 mrxjspfqyxx.ashx 仍可抓到
- **本机调试产物入 `.gitignore`**：`diag_ui.cjs` / `diag_*.cjs` / `static/daily_wangqian.csv.full_backup` 加入忽略列表，避免 dev 阶段产物污染 git status

## 不修（产品设计意图）

- 行情页「工具」入口：v1.121.137 changelog 明说"数据工具独立页与首屏折叠（设置入口）"
- 首页"值得看看"卡片标题 vs 楼盘名：v1.122.0 重构后的派生态位设计

## 验证

- 本机 `npm run dev:h5` + Playwright 截图：搜索框对齐 ✅、行情页 eyebrow ✅
- 本机 `python scripts/crawl_daily_wangqian.py fetch`：exit 0，CSV 1085 行（+12 广州 2026-09-08）
- OTA 推送：等 CI 自动跑 `build-app-wgt`，约 3-5 分钟出 v1.122.2 (281) 清单；用户 App 点"检查更新"即可拿到

## 来源

- git log：4cd315c、c51e838