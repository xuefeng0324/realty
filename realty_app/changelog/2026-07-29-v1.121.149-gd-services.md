# v1.121.149 — 广东规上服务业专栏

## 对照

- 广东统计局 tjkx185「规模以上服务业运行简况」HTML 正文可解析，此前未建卡
- 门类含「租赁和商务服务业」「房地产（不含房地产开发）」——住房弱相关、≠房价三轴
- 部分期次在「营业收入」与「同比增长」之间插入绝对额（如 5.54 万亿元）

## 改动

1. `crawl_gd_services.py` + `static/gd_services.csv`（回填约 10 期）
2. `gdServices.ts` + `macro-region` 新卡（`data-gd-services` / housing 分项）
3. 周更 CI 纳入 `gd_services.csv`
4. DATA_SOURCES / FEATURE_CATALOG 登记

## 验收

| 类型 | 期望 |
|------|------|
| 逻辑 | 最新期 2026_01_05 营收同比 7.3%、租赁商务 11.1%；2025 全年房地产服务 0.3%；1—11 月绝对额句式营收 6.7% |
| UI | macro-region 有 `data-gd-services` 与 `data-gd-services-housing`；脚注明示不含开发、≠房价 |
| 功能 | 周更 workflow 调用 `crawl_gd_services.py` 且会 git add `gd_services.csv` |

## 验证

- type-check / npm test ✅
- versionCode **270**
