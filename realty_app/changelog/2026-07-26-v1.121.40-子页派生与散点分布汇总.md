# v1.121.40 — 子页派生接线与散点/分布/地铁汇总

**日期**：2026-07-26  
**versionName**：1.121.40 · **versionCode**：161

## 变更

### 网签页 `wangqian.vue`

- 区级周趋势卡：修正 `CityCategoryTrend` / `DistrictSpike` 字段绑定（`recentAvg` / `multiplier`）
- 补 `trend-up` / `trend-down` 样式

### 70 城页 `stats70.vue`

| API | UI |
|-----|-----|
| `getStats70CurrentCityNationalRank` | 当前关注城全国排位 |
| `getStats70CityTrendDirection` | 近月涨跌方向 |
| `getStats70CityOver12MonthChange` | 近 12 月同比条 |
| `getStats70CrossCityByMonthSpread` | 最新月全国离散度（min–max） |

关注城默认跟 App `cityId`（广/深/珠），点榜可切换。

### 小区详情 `community.vue`

- 展示本小区 `listing_freshness`：新鲜度分 / 中位龄 / 近 4 周·近 2 周·陈旧套数

### 仪表盘

| API | UI |
|-----|-----|
| `summarizeCommunityScatterByCity` (+ quadrant / areaCohort) | 散点卡：本市聚合 + 象限/面积段计数 |
| `summarizeDistributionByCity` | 户型分布卡：跨城加权中位价 |
| `summarizeMetroPlanningByOpenYear` | 规划地铁：全国开通年份时间线 |

## 验证

- type-check ✅
- vitest 786/786 ✅
- E2E smoke ⚠️ 未跑
