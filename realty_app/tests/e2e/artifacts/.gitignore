# Playwright E2E artifacts (generated by smoke.mjs / visual-diff.mjs)
# baseline.png IS committed (visual regression anchor)
# smoke.png / smoke.json / diff.png are per-run, ignored
*.png
!baseline.png
*.json
!*.gitkeep